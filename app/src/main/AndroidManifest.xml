<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.sdwu.android02.homework.sn170507180218">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="杨谨安"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".HomeworkActivity2" />

        <service
            android:name=".MediaService"
            android:exported="false" />

        <activity android:name=".HomeworkActivity3" />
        <activity android:name=".HomeworkActivity1" />
        <activity android:name=".HomeworkActivity" />
    </application>

</manifest>